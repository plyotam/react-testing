import React from 'react';

const ConfigPanel: React.FC<any> = (props) => {
  return (
    <div {...props} style={{width: '300px', borderLeft: '1px solid #ccc', padding: '10px'}}>
      Config Panel Placeholder
    </div>
  );
};

export default ConfigPanel; 